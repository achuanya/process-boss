<Page
    x:Class="ProcessBoss.Views.RulesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProcessBoss.Views"
    xmlns:models="using:ProcessBoss.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource AppBackground}">
        <ScrollViewer Padding="0,0,0,24" Canvas.ZIndex="0">
            <Grid Margin="24,24,24,0" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>    <!-- Rules List -->
                    <RowDefinition Height="Auto"/> <!-- Status -->
                </Grid.RowDefinitions>

                <!-- Rules List -->
                <ListView Grid.Row="0" x:Name="RulesListView" ItemsSource="{x:Bind Rules}" SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,12" />
                            <Setter Property="MinHeight" Value="0" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ProcessRule">
                            <Border CornerRadius="12" BorderThickness="1" Padding="16"
                                    Background="{ThemeResource CardBackground}"
                                    BorderBrush="{ThemeResource CardBorder}"
                                    DoubleTapped="Item_DoubleTapped"
                                    RightTapped="Item_RightTapped"
                                    PointerEntered="RuleItem_PointerEntered"
                                    PointerExited="RuleItem_PointerExited"
                                    Tag="{x:Bind}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Hover/Selected visual overlay -->
                                    <Border Grid.ColumnSpan="2" Background="{ThemeResource ListHoverBackground}" CornerRadius="12" Margin="-16" IsHitTestVisible="False" Opacity="0" x:Name="HoverOverlay"/>

                                    <StackPanel Spacing="4">
                                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                            <TextBlock Text="{x:Bind ProcessName}" FontWeight="SemiBold" Foreground="{ThemeResource TextPrimary}" VerticalAlignment="Center" />
                                            <TextBlock Text="{x:Bind Remarks}" FontSize="12" Foreground="{ThemeResource TextSecondary}" VerticalAlignment="Center" Visibility="{x:Bind Remarks, Converter={StaticResource StringVisibilityConverter}, ConverterParameter=NotEmpty}" TextTrimming="CharacterEllipsis" MaxWidth="200" />
                                        </StackPanel>
                                        <TextBlock Text="{x:Bind FullPath}" Foreground="{ThemeResource TextSecondary}" FontSize="12" TextTrimming="CharacterEllipsis" MaxLines="1" />
                                        
                                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                                            <StackPanel.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{ThemeResource RuleItemTagBackground}"/>
                                                    <Setter Property="CornerRadius" Value="6"/>
                                                    <Setter Property="Padding" Value="8,4"/>
                                                </Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="FontSize" Value="11"/>
                                                    <Setter Property="Foreground" Value="{ThemeResource TextSecondary}"/>
                                                </Style>
                                            </StackPanel.Resources>

                                            <!-- Priority -->
                                            <Border Visibility="{x:Bind Priority, Converter={StaticResource PrioritySetConverter}}">
                                                <TextBlock Text="{x:Bind Priority, Converter={StaticResource ProcessPriorityConverter}}" />
                                            </Border>
                                            <!-- Efficiency -->
                                            <Border Visibility="{x:Bind EnableEfficiencyMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Text="效率模式" />
                                            </Border>
                                            <!-- Affinity -->
                                            <Border Visibility="{x:Bind CpuAffinityMask, Converter={StaticResource CpuAffinitySetConverter}}">
                                                <TextBlock Text="{x:Bind CpuAffinityMask, Converter={StaticResource CpuAffinityConverter}}" />
                                            </Border>
                                            <!-- Dynamic Boost -->
                                            <Border Visibility="{x:Bind EnableDynamicThreadPriorityBoost, Converter={StaticResource DynamicBoostSetConverter}}">
                                                <TextBlock Text="{x:Bind EnableDynamicThreadPriorityBoost, Converter={StaticResource DynamicBoostConverter}}" />
                                            </Border>
                                            <!-- IO -->
                                            <Border Visibility="{x:Bind IoPriority, Converter={StaticResource IoPrioritySetConverter}}">
                                                <TextBlock>
                                                    <Run Text="I/O: " /><Run Text="{x:Bind IoPriority, Converter={StaticResource IoPriorityConverter}}" />
                                                </TextBlock>
                                            </Border>
                                            <!-- Memory -->
                                            <Border Visibility="{x:Bind MemoryPriority, Converter={StaticResource MemoryPrioritySetConverter}}">
                                                <TextBlock>
                                                    <Run Text="内存: " /><Run Text="{x:Bind MemoryPriority, Converter={StaticResource MemoryPriorityConverter}}" />
                                                </TextBlock>
                                            </Border>
                                            <!-- GPU -->
                                            <Border Visibility="{x:Bind GpuPriority, Converter={StaticResource GpuPrioritySetConverter}}">
                                                <TextBlock>
                                                    <Run Text="GPU: " /><Run Text="{x:Bind GpuPriority, Converter={StaticResource GpuPriorityConverter}}" />
                                                </TextBlock>
                                            </Border>
                                            
                                            <!-- Kill (Red) -->
                                            <Border Visibility="{x:Bind IsKillSet, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Text="自动结束" />
                                            </Border>
                                            
                                            <!-- Default Config -->
                                            <Border Visibility="{x:Bind IsDefaultConfig, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock Text="默认配置" />
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                        <ToggleSwitch IsOn="{x:Bind IsEnabled, Mode=TwoWay}" OffContent="关" OnContent="开" MinWidth="0" Tag="{x:Bind}" Toggled="RuleToggle_Toggled" Foreground="{ThemeResource ToggleOnBrush}" FlowDirection="RightToLeft"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Grid.Row="1" x:Name="StatusTextBlock" Text="就绪" Foreground="{ThemeResource TextSecondary}" FontSize="12" HorizontalAlignment="Center" Margin="0,20" />
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
