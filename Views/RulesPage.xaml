<Page
    x:Class="SKM.Views.RulesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SKM.Views"
    xmlns:models="using:SKM.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <ScrollViewer Padding="0,0,0,24" Canvas.ZIndex="0">
            <Grid Margin="24,24,24,0" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Rules Header -->
                    <RowDefinition Height="*"/>    <!-- Rules List -->
                    <RowDefinition Height="Auto"/> <!-- Status -->
                </Grid.RowDefinitions>

                <!-- Rules Header -->
                <Grid Grid.Row="0" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="规则列表" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="0,0,0,2" />
                    <Button Grid.Column="1" Click="AddButton_Click" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" CornerRadius="6" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE710;" FontSize="14" />
                            <TextBlock Text="添加规则" />
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- Rules List -->
                <ListView Grid.Row="1" x:Name="RulesListView" ItemsSource="{x:Bind Rules}" SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,12" />
                            <Setter Property="MinHeight" Value="0" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ProcessRule">
                            <Border CornerRadius="12" BorderThickness="1" Padding="16"
                                    Background="{StaticResource CardBackgroundBrush}"
                                    BorderBrush="{StaticResource CardBorderBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Spacing="4">
                                        <TextBlock Text="{x:Bind ProcessName}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                                        <TextBlock Text="{x:Bind FullPath}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" TextTrimming="CharacterEllipsis" MaxLines="1" />
                                        
                                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                                            <Border Background="#F2F2F7" CornerRadius="6" Padding="8,4">
                                                <TextBlock Text="{x:Bind Priority}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                                            </Border>
                                            <Border Background="#F2F2F7" CornerRadius="6" Padding="8,4" Visibility="{x:Bind EnableEfficiencyMode}">
                                                <TextBlock Text="效率模式" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                                            </Border>
                                             <Border Background="#FFF2F2" CornerRadius="6" Padding="8,4" Visibility="{x:Bind KillOnStart}">
                                                <TextBlock Text="自动结束" FontSize="11" Foreground="#FF3B30"/>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                        <Button Content="编辑" Click="EditButton_Click" Tag="{x:Bind}" Background="Transparent" BorderThickness="0" Foreground="{StaticResource AccentBrush}" />
                                        <Button Content="删除" Click="DeleteButton_Click" Tag="{x:Bind}" Background="Transparent" BorderThickness="0" Foreground="#FF3B30" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock Grid.Row="2" x:Name="StatusTextBlock" Text="就绪" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" HorizontalAlignment="Center" Margin="0,20" />
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
