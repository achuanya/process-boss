<ContentDialog
    x:Class="SKM.Views.RuleEditorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SKM.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="规则编辑"
    PrimaryButtonText="保存"
    SecondaryButtonText="取消"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <StackPanel Spacing="10" MinWidth="400">
        <TextBox x:Name="PathTextBox" Header="程序路径" PlaceholderText="C:\Path\To\App.exe" />
        <Button Content="浏览..." Click="BrowseButton_Click" />

        <TextBlock Text="优先级" Margin="0,10,0,0" />
        <RadioButtons x:Name="PriorityRadioButtons">
            <RadioButton Content="实时" Tag="RealTime" />
            <RadioButton Content="高" Tag="High" />
            <RadioButton Content="高于正常" Tag="AboveNormal" />
            <RadioButton Content="正常" Tag="Normal" IsChecked="True" />
            <RadioButton Content="低于正常" Tag="BelowNormal" />
            <RadioButton Content="低" Tag="Low" />
        </RadioButtons>

        <TextBlock Text="CPU 相关性" Margin="0,10,0,0" />
        <StackPanel Orientation="Horizontal" Spacing="10">
            <CheckBox x:Name="Cpu0Check" Content="CPU0" IsChecked="True" />
            <CheckBox x:Name="Cpu1Check" Content="CPU1" IsChecked="True" />
            <CheckBox x:Name="Cpu2Check" Content="CPU2" IsChecked="True" />
            <CheckBox x:Name="Cpu3Check" Content="CPU3" IsChecked="True" />
            <!-- In a real app, generate this dynamically based on Environment.ProcessorCount -->
        </StackPanel>
        
        <CheckBox x:Name="EfficiencyModeCheck" Content="启用效率模式" Margin="0,10,0,0" />
        
        <CheckBox x:Name="KillCheck" Content="启动后结束进程" />
        <CheckBox x:Name="KillTreeCheck" Content="启动后结束进程树" />
    </StackPanel>
</ContentDialog>
