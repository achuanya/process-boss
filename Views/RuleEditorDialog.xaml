<ContentDialog
    x:Class="ProcessBoss.Views.RuleEditorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProcessBoss.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="规则编辑"
    Style="{ThemeResource DefaultContentDialogStyle}">

    <Grid Width="400" Height="450" Background="{ThemeResource CardBackground}">
        <Grid x:Name="MainForm">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="0,0,16,0">
                <StackPanel Spacing="20" Padding="4">
                    <!-- Basic Info -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="PathTextBox" Header="程序路径" PlaceholderText="C:\Path\To\App.exe" Margin="0,0,8,0" />
                        <Button Content="浏览" Click="BrowseButton_Click" Grid.Column="1" VerticalAlignment="Bottom" />
                    </Grid>

                    <!-- CPU Performance -->
                    <StackPanel Spacing="12">
                        <TextBlock Text="CPU 性能配置" FontWeight="SemiBold" Foreground="{ThemeResource TextPrimary}" />
                        
                        <Grid ColumnSpacing="12" RowSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Priority -->
                            <StackPanel Spacing="4" Grid.Row="0" Grid.Column="0">
                                <TextBlock Text="CPU 优先级" FontSize="12" Foreground="{ThemeResource TextSecondary}"/>
                                <ComboBox x:Name="PriorityComboBox" HorizontalAlignment="Stretch" />
                            </StackPanel>

                            <!-- Affinity -->
                            <StackPanel Spacing="4" Grid.Row="0" Grid.Column="1">
                                <TextBlock Text="CPU 核心" FontSize="12" Foreground="{ThemeResource TextSecondary}"/>
                                <Button x:Name="AffinityButton" Content="所有处理器" HorizontalAlignment="Stretch" Click="ShowAffinityOverlay_Click" />
                            </StackPanel>
                            
                            <!-- Toggles -->
                            <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Spacing="8">
                                <CheckBox x:Name="EfficiencyModeCheck" Content="启用效率模式 (Eco Mode)" />
                                <CheckBox x:Name="DynamicBoostCheck" Content="允许 Windows 动态线程优先级提升" IsThreeState="False" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <!-- Other Resources -->
                    <StackPanel Spacing="12">
                        <TextBlock Text="其他资源优先级" FontWeight="SemiBold" Foreground="{ThemeResource TextPrimary}" />
                        
                        <Grid ColumnSpacing="12" RowSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- I/O -->
                            <StackPanel Spacing="4" Grid.Column="0">
                                <TextBlock Text="I/O 优先级" FontSize="12" Foreground="{ThemeResource TextSecondary}"/>
                                <ComboBox x:Name="IoPriorityComboBox" HorizontalAlignment="Stretch" />
                            </StackPanel>

                            <!-- Memory -->
                            <StackPanel Spacing="4" Grid.Column="1">
                                <TextBlock Text="内存优先级" FontSize="12" Foreground="{ThemeResource TextSecondary}"/>
                                <ComboBox x:Name="MemoryPriorityComboBox" HorizontalAlignment="Stretch" />
                            </StackPanel>

                            <!-- GPU -->
                            <StackPanel Spacing="4" Grid.Column="2">
                                <TextBlock Text="GPU 优先级" FontSize="12" Foreground="{ThemeResource TextSecondary}"/>
                                <ComboBox x:Name="GpuPriorityComboBox" HorizontalAlignment="Stretch" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                    
                    <!-- Execution Control -->
                    <StackPanel Spacing="12">
                        <TextBlock Text="执行控制" FontWeight="SemiBold" Foreground="{ThemeResource TextPrimary}" />
                        <StackPanel Orientation="Horizontal" Spacing="16">
                            <CheckBox x:Name="KillCheck" Content="结束进程" />
                            <CheckBox x:Name="KillTreeCheck" Content="结束进程树" />
                        </StackPanel>
                    </StackPanel>

                    <!-- Remarks (Moved) -->
                    <TextBox x:Name="RemarksTextBox" Header="备注" PlaceholderText="例如：游戏模式、办公优化" />
                </StackPanel>
            </ScrollViewer>

            <!-- Footer Buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button Content="保存" Click="SaveButton_Click" Width="80" Background="{ThemeResource AccentBrush}" Foreground="White" BorderThickness="0"/>
                <Button Content="取消" Click="CancelButton_Click" Width="80"/>
            </StackPanel>
        </Grid>

        <!-- Affinity Overlay -->
        <Grid x:Name="AffinityOverlay" Visibility="Collapsed" Background="{ThemeResource CardBackground}" CornerRadius="8" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Spacing="10" Margin="0,0,0,10">
                <TextBlock x:Name="AffinitySubtitle" Text="允许哪些处理器运行此程序？" Foreground="{ThemeResource TextSecondary}" Opacity="0.7"/>
                <CheckBox x:Name="AllProcessorsCheck" Content="所有处理器" Click="AllProcessors_Click"/>
                <Rectangle Height="1" Fill="{ThemeResource CardBorder}" />
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="AffinityStackPanel" Spacing="4">
                    <!-- Checkboxes will be added here dynamically -->
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="确定" Click="AffinityOk_Click" Width="80" Background="{ThemeResource AccentBrush}" Foreground="White" BorderThickness="0"/>
                <Button Content="取消" Click="AffinityCancel_Click" Width="80"/>
            </StackPanel>
        </Grid>
    </Grid>
</ContentDialog>